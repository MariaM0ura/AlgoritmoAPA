#include <iostream>
#include <vector>
#include <algorithm>
#include <ctime>
#include <cstdlib>
#include <chrono>

int main() {
    srand(time(0));  // Inicializa o gerador de números aleatórios


    // Parâmetros
    int max_iteracoes = 1000;
    int tamanho_solucao = 10;
    int num_execucoes = 10;
    
    return 0;
}
